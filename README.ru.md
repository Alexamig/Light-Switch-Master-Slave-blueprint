# Light / Switch Master-Slave Automation v5.0 FINAL STABLE

[![hacs_badge](https://img.shields.io/badge/HACS-Custom-orange.svg)](https://github.com/hacs/integration)
![GitHub release](https://img.shields.io/github/v/release/Alexamig/Light-Switch-Master-Slave-blueprint)

## 📄 Описание
Универсальный blueprint для Home Assistant для управления `light` и `switch` с полной fail-safe защитой на основе датчика движения и таймера.

### ⚡ Основные возможности
- Поддержка Master и любого количества Slave устройств
- Таймеры Master и Slave работают независимо
- Автоматическое включение по движению (если разрешено)
- Работает с режимом День / Ночь (опционально)
- Поддержка смешанных доменов: `light` и `switch`
- Игнорирование времени суток для отдельных Slave
- Fail-safe защита при недоступности сенсоров

## 🧩 Входные параметры (Inputs)
| Input | Описание |
|-------|----------|
| `🧍 Реакция на движение` | Включение/отключение авто-реакции на движение |
| `🏃 Датчик движения` | Обязательный binary_sensor |
| `Основной (💡Light/🎚️Switch)` | Master устройство |
| `⏱️ Таймер (Master)` | Таймер Master |
| `⏲️ Таймер (Slave)` | Таймер Slave (может быть тем же что и Master) |
| `🔄 Синхронизация` | Включение работы Slave как группы |
| `Дополнительно (💡Light/🖲️Switch)` | Slave устройства |
| `🔕 Игнорировать время суток (Slave)` | Работает только при отключенной синхронизации |
| `🌞День/🌜Ночь` | Binary sensor для режима день/ночь |
| `Режим (🔆День/🕯️Ночь)` | Использовать sensor день/ночь |
| `🌗 Инверсия → (день ↔ ночь)` | Инвертировать логику дня/ночи |
| `⏳ Задержка ПЕРЕД движением` | Дебаунс до движения |
| `⏳ Задержка ПОСЛЕ движения` | Дебаунс после движения |
| `Установка таймера Master` | Время работы Master таймера |
| `Установка таймера Slave` | Время работы Slave таймера |

## 🕹 Логика работы
```text
1️⃣ Motion ON:
   - Включение Master и/или Slave если разрешено
   - Отмена активных таймеров

2️⃣ Motion OFF:
   - Запуск таймеров Master и Slave по окончании движения
   - Если таймер один для Master и Slave, используется только один

3️⃣ Night Sensor Changed:
   - Авто-включение/выключение Master и Slave при смене дня/ночи

4️⃣ Motion unavailable:
   - Fail-safe: запуск таймеров при недоступности сенсора

5️⃣ Master changed:
   - Ручное управление Master
   - Включение/выключение синхронизированных Slave

6️⃣ Slave changed:
   - Ручное управление Slave
   - Таймеры запускаются при отключенной синхронизации

7️⃣ Master timer finished:
   - Выключение Master и всех включенных Slave (если синхронизация или общий таймер)

8️⃣ Slave timer finished:
   - Выключение всех включенных Slave (при отдельном таймере)

9️⃣ Safety check каждые 5 минут:
   - Контроль состояний и запуск таймеров при недоступности сенсоров
```

## ⚙️ Настройка таймеров
- Один таймер можно использовать для Master и Slave одновременно
- Если два таймера, Master управляет Master + все Slave (при синхронизации)
- Таймер Slave управляет только Slave при отключенной синхронизации

## 🔄 Синхронизация и Day/Night
- При включенной синхронизации все Slave работают как группа
- При отключенной синхронизации Slave могут игнорировать время суток
- Поддержка смешанных доменов (`light` и `switch`)

## 🛠 Fail-safe
- Проверка состояния Motion каждые 5 минут
- Запуск таймеров при недоступности сенсора



